# 列表列超长省略审计报告

> 审计时间：2026-09-25
> 触发问题：日志列表「描述」列全量显示导致单元格换行、行高被撑开
> 审计范围：`src/views/**` 共 30 个定义表格列的文件、310 个列定义
> 结论：**省略机制全项目只做了约 3/4（29 个表格中 6 个缺失）**，属「漏配」而非「设计取舍」
> 落地状态：**✅ 2026-09-25 已全部修复并提交（commit `e4e4748`）**，表格级省略覆盖率 **29/29**

---

## 一、根因（机制层，已核对 Element Plus 源码）

| 环节         | 源码 / 样式                                                     | 效果                                                                               |
| ------------ | --------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 默认单元格   | `.el-table .cell{white-space:normal;overflow-wrap:break-word}`  | **默认允许换行** → 长文本把行撑高                                                  |
| 开启省略后   | `.el-table .cell.el-tooltip{white-space:nowrap;min-width:50px}` | 强制单行 + `overflow:hidden` + `text-overflow:ellipsis`                            |
| 列级继承规则 | `table-column/index.mjs:48`                                     | 列未显式声明时**继承表格级**；显式 `false` 可覆盖；`type="selection"` 恒为 `false` |

**结论**：在 `<pure-table>` 上加表格级 `showOverflowTooltip` 是项目既定的统一做法（**17 个列表页 + 6 个 provider-health 子表格已用**）。日志相关页面**漏加了这个属性**，因此 `.cell` 保持 `white-space:normal`，描述类长文本自然换行 —— 这是本次问题的直接原因，而非样式冲突。

---

## 二、现状量化（修复前基线）

| 指标                                        | 数值                              |
| ------------------------------------------- | --------------------------------- |
| 定义表格列的文件                            | 30 个                             |
| 列定义总数                                  | 310 列                            |
| 已开**表格级** `showOverflowTooltip` 的表格 | 23 个（17 列表页 + 6 组件表格）   |
| 未开省略的表格                              | **6 个** → 修复后 **0 个**        |
| 仅做**列级**精准省略的                      | `cache-monitor`（4 处，唯一范例） |
| 列级与表格级**重复**声明的                  | 8 处（无害冗余）                  |

---

## 三、缺口清单（长文本裸奔，建议补齐）

### P0 — 日志模块 5 页 + 缓存管理页（✅ 已修复，commit `e4e4748`）

| 页面         | 文件（补属性的位置）          | 裸奔的高价值长文本列                                                                                                |
| ------------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 任务执行日志 | `logs/operation/index.vue:53` | **描述**(200)、**调用方法**(260)、任务名                                                                            |
| 系统日志     | `logs/system/index.vue:66`    | **请求描述**(140)、**请求接口**(140)、请求方法(140)、TraceId(180)、请求来源(140)、地点(140)、资源ID、业务ID、操作人 |
| 登录日志     | `logs/login-log/index.vue:43` | **描述**(160)                                                                                                       |
| 邮件发送记录 | `logs/login/index.vue:48`     | **主题**(无 minWidth)                                                                                               |
| 版本发布日志 | `logs/version/index.vue:51`   | 分支(90)                                                                                                            |
| 缓存管理     | `monitor/cache/index.vue:103` | **缓存 Key**(300)                                                                                                   |

> ✅ 2026-09-25 已落地（commit `e4e4748`）：上述 6 处均在 `align-whole="center"` 后补 `showOverflowTooltip`，
> 省略覆盖率 **29/29**，改动共 6 行；`pnpm typecheck`、ESLint `--max-warnings 0` 均通过。
> 行号可用于回溯改动位置。

### 附带发现（同页顺带修正）

| 位置                      | 问题                                                                                            | 建议                     |
| ------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------ |
| `logs/login/hook.tsx:113` | 发送时间列 `minWidth: 300`，内容仅 19 字符日期，列被无谓撑宽                                    | 降到 `170`               |
| `logs/system` 21 列       | 列数过多 + `table-layout="auto"`，横向滚动严重                                                  | 省略 + 适度收窄 minWidth |
| 目录命名                  | `logs/login/` 实为「邮件发送记录」（`useMailLog`），与 `logs/login-log/`（真·登录日志）语义冲突 | 列入重命名待办，本次不改 |

---

## 四、字段级判断规则（哪些该省略、哪些不该）

### ✅ 应当省略（自由文本，长度不可控）

`描述` `说明` `备注` `标题` `主题` `内容` `预览(Prompt/Response)` `错误信息` `故障原因` `请求接口/URI` `组件路径` `路由路径` `权限标识` `TraceId` `缓存 Key` `配置 Key/值` `邮箱` `样例输入` `调用方法` —— 统一 `showOverflowTooltip` + hover 查看全文。

### ⚪ 无需省略（但整表开启无害）

短枚举、状态 tag、时间、数字/占比、ID、操作列（`slot` 不受影响）。

### ⛔ 明确不应省略（需保留多行结构）

- `notice/sysNotice` 的公告内容/优先级/平台/状态/时间列：`cellRenderer` 内是**双行 div 结构**（`notice-title-cell` 等），且 `index.vue:271/281` 已自带 ellipsis 样式 —— 保持现状，属成熟范式。
- `cache-monitor` 三个看板内表格：用**列级精准省略**（Key / 描述列），是项目里唯一按列判断的实现，可作参考。

---

## 五、反向核查结论

抽查全部 23 个已开省略的表格，未发现"因 nowrap 导致多行布局被压坏"的情况：多行类单元格均为 block 结构 + 自带省略样式，`nowrap` 只作用于行内文本。**反向风险为 0**，可安全以整表开启作为统一基线。

---

## 六、落地建议

**P0（修问题）✅ 已完成（commit `e4e4748`）**：6 个文件补表格级 `showOverflowTooltip`，覆盖率 29/29。
本次未发现需要显式 `showOverflowTooltip: false` 的列（日志与缓存页的 `cellRenderer` 均为单行 tag/文本）。

**P1（去冗余 / 规范）**：清理 8 处与表格级重复的列级 `showOverflowTooltip: true`；长文本列 `minWidth` 收敛到 `120~200`，避免 260/300 这类过宽值（对应第 50 行「附带发现」）。

**P2（治本）**：把"`<pure-table>` 默认开启 `showOverflowTooltip`"写入列表页规范（现状已是 29/29 全覆盖），或在 `PureTableBar` / 封装层设默认值，使新页面无需每次手写。

---

## 附：审计方法（可复现）

- `columns` 定义抽取脚本：`.workbuddy/tmp/scan_columns.py`，输出 `.workbuddy/tmp/columns.txt`
- 属性使用检索：`show-overflow-tooltip`（kebab，模板级）与 `showOverflowTooltip`（camel，列级）
- 机制验证：`node_modules/element-plus` 中 `table-column/index.mjs`、`table-layout.mjs`、`dist/index.css`
