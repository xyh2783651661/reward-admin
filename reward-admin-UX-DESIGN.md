# 新增核心阶段：页面级功能逆向工程与操作逻辑深度挖掘

前面的系统级、模块级分析完成后，必须进入：

> **页面级功能逆向工程阶段**

本阶段是整个项目分析中最重要的阶段之一。

禁止只根据：

- 页面名称
- 菜单名称
- 文件名
- 路由名称

对页面进行简单总结。

必须真正阅读页面相关的：

```text
Vue 页面
组件
子组件
API
Hooks
Store
Router
权限逻辑
数据处理
事件处理
```

通过代码反向推导：

> **这个页面到底展示了什么？**

> **用户可以做什么？**

> **用户点击每一个操作后会发生什么？**

> **这些操作最终影响什么数据？**

> **页面之间如何关联？**

---

# 一、分析粒度必须下钻到页面内部

禁止只输出：

> 用户管理页面用于管理用户。

必须进一步拆解：

```text
用户管理页面
│
├── 页面进入
│   ├── 自动请求什么数据
│   ├── 默认展示什么
│   ├── 是否读取缓存
│   └── 是否读取路由参数
│
├── 查询区域
│   ├── 有哪些字段
│   ├── 每个字段作用
│   ├── 查询参数如何传递
│   ├── 查询后页面如何变化
│   └── 重置后如何恢复
│
├── 数据展示区域
│   ├── 展示哪些字段
│   ├── 字段来源
│   ├── 字段格式化
│   ├── 状态展示
│   └── 数据为空时如何处理
│
├── 行操作
│   ├── 查看
│   ├── 编辑
│   ├── 删除
│   ├── 启用
│   └── 其他操作
│
├── 批量操作
│
├── 新增操作
│
├── Dialog / Drawer / 子页面
│
└── 页面状态
    ├── Loading
    ├── Empty
    ├── Error
    ├── Success
    └── Permission
```

具体结构必须以实际代码为准。

---

# 二、必须建立“页面功能档案”

对于每一个核心页面，单独建立一份：

> **页面功能档案**

格式必须统一。

---

## 【页面基本信息】

```text
页面名称：

路由：

对应菜单：

对应 Vue 文件：

关联组件：

关联 API：

关联权限：

关联 Store：

主要用户角色：
```

如果无法确定：

> 标记【无法从当前代码确认】。

---

# 三、页面存在的目的

必须回答：

> 用户为什么需要进入这个页面？

不要简单回答：

> 因为菜单中有这个功能。

而是分析：

```text
用户目标
↓
用户需要完成什么任务
↓
系统提供什么能力
↓
当前页面如何帮助用户完成
```

例如：

```text
用户目标：

管理员需要快速找到某个用户，
查看用户状态，
修改用户信息，
或者禁用异常用户。

当前页面能力：

查询
筛选
查看
新增
编辑
启用
禁用
删除
```

所有内容必须根据实际代码判断。

---

# 四、页面首次进入行为分析

必须分析用户第一次进入页面时：

```text
进入页面
↓
触发什么生命周期
↓
调用哪些方法
↓
请求哪些 API
↓
初始化哪些数据
↓
默认展示什么
```

重点检查：

- onMounted / created
- watch
- 路由参数
- Store
- LocalStorage
- SessionStorage
- 缓存
- 默认查询参数

输出：

```text
【页面初始化流程】

用户进入页面

↓
初始化查询参数

↓
加载 xxx

↓
请求 API：

GET /xxx

↓
数据处理：

xxx

↓
最终展示：

xxx
```

---

# 五、逐个分析页面展示区域

页面不是一个整体。

必须按照实际 UI 区块进行拆分。

例如：

```text
页面
│
├── 页面标题 / Breadcrumb
├── 操作栏
├── 查询区域
├── 数据统计区域
├── Tab
├── 表格
├── 分页
├── Dialog
├── Drawer
└── Footer
```

每一个区域都必须回答：

```text
这个区域展示什么？

为什么展示？

数据来自哪里？

用户可以做什么？

用户操作后发生什么？

是否存在状态变化？

是否存在体验问题？
```

---

# 六、查询区域深度分析

不能只说：

> 页面有查询功能。

必须逐个分析查询字段。

格式：

| 查询字段 | 展示名称 | 数据类型 | 用户作用 | API 参数 | 默认值 | 是否高频 | 是否合理 |
| -------- | -------- | -------- | -------- | -------- | ------ | -------- | -------- |

例如：

```text
用户名
↓
用户输入
↓
绑定 searchForm.username
↓
点击查询
↓
转换请求参数
↓
调用 getUserList()
↓
刷新 tableData
```

同时分析：

- 是否支持 Enter 查询
- 是否自动查询
- 是否防抖
- 查询是否重置页码
- 重置逻辑是否完整
- 查询条件是否过多
- 是否存在无意义字段
- 是否存在用户无法理解的条件

---

# 七、数据展示深度分析

对于：

- Table
- Card
- Tree
- List
- Chart
- Detail

必须逐项分析。

例如 Table：

| 展示字段 | 数据来源 | 格式处理 | 用户价值 | 是否核心 | 是否存在展示问题 |
| -------- | -------- | -------- | -------- | -------- | ---------------- |

不能只说：

> 表格展示用户信息。

必须分析：

```text
用户 ID
用户名
昵称
状态
创建时间
更新时间
操作
```

并回答：

> 为什么用户需要看这个字段？

> 这个字段是否应该默认展示？

> 是否存在重要字段被隐藏？

> 是否存在用户根本不关心的字段占据大量空间？

---

# 八、逐个分析每一个操作

这是本阶段最重要的要求。

对于页面上的：

- Button
- Icon
- Dropdown
- Link
- Row Action
- Batch Action
- Context Menu
- Tab
- Switch
- Checkbox
- Upload
- Download

必须尽可能逐个分析。

格式：

```text
【操作名称】

展示位置：

触发条件：

权限条件：

用户点击后：

↓
执行方法：

↓

数据校验：

↓

调用 API：

↓

返回结果：

↓

页面状态变化：

↓

成功反馈：

↓

失败反馈：

↓

是否存在重复提交：

↓

是否存在误操作风险：
```

例如：

```text
【删除用户】

用户点击删除

↓
是否弹出确认框

↓
确认后

调用：

deleteUser(id)

↓

API：

DELETE /user/{id}

↓

成功：

提示删除成功

↓

重新请求列表

↓

失败：

提示错误
```

必须根据真实代码分析。

如果某一步代码中不存在：

> 明确标记不存在。

---

# 九、必须生成“操作地图”

每一个核心页面必须生成：

```text
页面
│
├── 查询
│   ├── 输入关键词
│   ├── 选择状态
│   └── 点击查询
│
├── 数据操作
│   ├── 查看
│   ├── 编辑
│   ├── 删除
│   └── 状态修改
│
├── 新增
│   └── 打开表单
│
└── 批量操作
```

然后继续展开。

例如：

```text
编辑
↓
打开 Dialog
↓
获取当前数据
↓
数据回显
↓
修改字段
↓
表单校验
↓
提交
↓
调用 API
↓
成功提示
↓
关闭 Dialog
↓
刷新列表
```

最终形成：

> **页面操作地图**

---

# 十、分析隐藏逻辑

除了页面直接看到的内容，还必须主动挖掘：

- v-if
- v-show
- 权限判断
- computed
- watch
- 生命周期
- 路由参数
- Store
- LocalStorage
- SessionStorage

特别关注：

> 用户看不到，但是会影响页面行为的逻辑。

例如：

```text
管理员
↓
显示删除按钮

普通用户
↓
隐藏删除按钮
```

或者：

```text
status = 1
↓
显示审核按钮

status = 2
↓
显示发布按钮
```

这些都必须记录。

---

# 十一、页面状态机分析

对于复杂页面，建立页面状态变化。

例如：

```text
初始状态
↓
Loading
↓
加载成功
↓
正常展示

或者

↓
加载失败
↓
Error
```

操作状态：

```text
编辑
↓
编辑中
↓
校验失败
↓
继续编辑

或者

提交
↓
Loading
↓
成功
↓
刷新列表
```

必须检查：

- Loading 是否明确
- 是否允许重复提交
- 失败后是否能恢复
- 页面数据是否正确刷新
- Dialog 是否正确关闭
- 表单是否正确重置

---

# 十二、页面之间的跳转关系

不能孤立分析页面。

必须主动寻找：

```text
页面 A
↓
点击详情
↓
页面 B
↓
编辑
↓
页面 C
↓
返回
↓
页面 A
```

分析：

- 参数传递
- 数据传递
- 返回行为
- 页面缓存
- KeepAlive
- 页面刷新

建立：

> **页面关系图**

重点寻找：

> 用户完成一个任务是否需要频繁在多个页面之间跳转。

---

# 十三、建立完整用户任务流

不要只分析按钮。

必须模拟用户任务。

例如：

```text
任务：

修改某条数据。

当前操作：

进入模块
↓
输入查询条件
↓
查询
↓
找到目标数据
↓
点击编辑
↓
修改
↓
保存
↓
刷新
```

统计：

```text
页面跳转：1

点击次数：4

需要填写字段：3

API 请求：2

等待次数：2
```

然后设计优化方案。

---

# 十四、每个页面必须回答的核心问题

每一个核心页面最终必须回答：

### 1.

用户为什么进入这个页面？

### 2.

用户最主要的任务是什么？

### 3.

用户进入页面后第一眼看到什么？

### 4.

用户第一步应该做什么？

### 5.

页面是否明确告诉用户可以做什么？

### 6.

页面有哪些展示区域？

### 7.

每个区域的数据来自哪里？

### 8.

用户可以进行哪些操作？

### 9.

每个操作最终会影响什么？

### 10.

操作成功后页面如何变化？

### 11.

操作失败后用户如何继续？

### 12.

是否存在隐藏操作或条件操作？

### 13.

是否存在用户容易误解的地方？

### 14.

是否存在用户需要多次点击才能完成的操作？

### 15.

是否存在重复输入？

### 16.

哪些信息应该突出？

### 17.

哪些信息应该隐藏或折叠？

### 18.

这个页面最大的用户体验问题是什么？

### 19.

这个页面最大的代码问题是什么？

### 20.

这个页面最值得做的优化是什么？

---

# 十五、禁止“页面一句话总结”

以下行为禁止：

```text
用户管理：

用于用户的增删改查，
建议优化查询和交互。
```

这是无效分析。

必须至少输出：

```text
页面目标

↓

页面结构

↓

初始化行为

↓

数据来源

↓

查询逻辑

↓

展示字段

↓

每个操作

↓

状态变化

↓

权限控制

↓

关联页面

↓

完整用户任务流

↓

问题

↓

优化建议
```

对于核心业务页面：

> 必须进行完整拆解。

---

# 十六、页面分析完成后生成“页面功能矩阵”

最终生成：

| 页面 | 核心任务  | 操作数量 | API 数量 | 复杂度 | 用户体验风险 | 技术风险 |
| ---- | --------- | -------- | -------- | ------ | ------------ | -------- |
| xxx  | 查询/管理 | 12       | 6        | 高     | 高           | 中       |
| xxx  | 编辑      | 8        | 3        | 中     | 中           | 高       |

然后筛选：

> 最值得优先优化的 Top 10 页面。

---

# 十七、页面问题必须能够追溯到代码

最终形成：

```text
用户问题

↓

页面交互问题

↓

具体操作问题

↓

Vue 组件

↓

方法

↓

API

↓

具体代码位置
```

例如：

```text
用户频繁重复输入

↓

编辑页面没有自动带入关联数据

↓

EditDialog.vue

↓

handleOpen()

↓

没有请求关联数据

↓

需要增加：

getDetail(id)
```

---

# 十八、页面级分析完成后才允许设计新交互

在没有完成页面功能挖掘之前：

> 不允许直接提出大规模 UI 改版。

必须先：

```text
理解当前页面
↓
理解当前操作
↓
理解用户任务
↓
找到真实问题
↓
再设计新的交互
```

---

# 十九、最终输出格式

对于每个核心页面：

```text
# 页面：xxx

## 1. 页面目的

## 2. 目标用户

## 3. 核心任务

## 4. 页面结构

## 5. 初始化流程

## 6. 数据来源

## 7. 查询逻辑

## 8. 展示内容分析

## 9. 操作列表

## 10. 每个操作的完整逻辑

## 11. 权限与隐藏逻辑

## 12. 页面状态

## 13. 关联页面

## 14. 用户任务流

## 15. 当前体验问题

## 16. 当前技术问题

## 17. 优化机会

## 18. 推荐交互方案

## 19. 涉及代码改动

## 20. 优先级
```

对于简单页面，可以适当简化。

对于复杂核心页面：

> 必须完整分析，不能省略操作逻辑和展示细节。
